const Up = Symbol("Up");
const Down = Symbol("Down");
const Left = Symbol("Left");
const Right = Symbol("Right");
const Interact = Symbol("Interact");

export const Input = {
  Down,
  Interact,
  Left,
  Right,
  Up,
};
